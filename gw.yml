server:
  port: 8762
  
spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: studentsvc-route
              uri: lb://studentsvc
              predicates:
                - Path=/student/**
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
